// script.js - Ficheiro JavaScript externo para fase2.html
// Este ficheiro é carregado via <script src="script.js" async> no <head>

// Exemplo simples: exibe uma mensagem na consola quando o script é carregado
console.log("script.js carregado com sucesso! (modo: async)");

// Nota: como este script usa 'async', pode ser executado antes do DOM estar
// completamente carregado. Por isso, evitamos manipular o DOM aqui diretamente.
// Para manipular o DOM, usaríamos 'defer' ou envolvíamos o código num evento:
// document.addEventListener('DOMContentLoaded', function() { ... });
